target/
out/
tmp-sloc/
*.log
*.tmp
.git/
.vscode/
.idea/
# Uncompressed vendor tree — Docker always extracts from vendor.tar.xz instead,
# so the directory must be excluded to prevent COPY from shadowing the archive.
vendor/
