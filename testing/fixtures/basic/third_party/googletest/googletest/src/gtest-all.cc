// gtest-all.cc — stub translation unit for the demo fixture.
// In the real googletest source this file includes every gtest-*.cc
// implementation file so that a project can build gtest with a single
// compilation unit.  This stub is here only to give the CMake target
// something to compile; all logic lives in the header for this demo.
#include "gtest/gtest.h"
